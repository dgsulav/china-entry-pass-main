import React from "react";
import spinner from "../../assets/spinner.gif";
const Spinner = () => {
  return (
    <img
      src={spinner}
      alt="spinner"
      // color={color}
      height="50px"
    />
  );
};

export default Spinner;
